<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      font-family:'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
    }
  </style>
  <title>Library App</title>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">Library App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarContent">
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/addBook"><i class="fas fa-plus me-1"></i>Add Book</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt me-1"></i>Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center"><h2>Books in Library</h2></div>
    <br>
    <table class="table table-bordered text-center">
      <thead>
        <tr>
          <th>Book Cover</th>
          <th>Title</th>
          <th>ISBN</th>
          <th>Author</th>
          <th>Edit</th>
          <th>Delete</th>
        </tr>
      </thead>
        <tbody>
        <% for (let i = 0; i < books.length; i++) { %>
          <tr>
            <td>
              <img src="<%= books[i].image_url %>" alt="Cover" class="img-thumbnail" style="width: 80px; height: auto;">
            </td>
            <td><%= books[i].title %></td>
            <td><%= books[i].isbn %></td>
            <td><%= books[i].author %></td>
            <td>
              <a href="/updateBook/<%= books[i].book_id %>" class="btn btn-outline-dark btn-sm">
                <i class="fas fa-edit"></i> Edit
              </a>
            </td>
            <td>
              <a href="/deleteBook/<%= books[i].book_id %>" class="btn btn-outline-dark btn-sm" onclick="return confirm('Are you sure you want to delete this book?')">
                <i class="fas fa-trash-alt"></i> Delete
              </a>
            </td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
